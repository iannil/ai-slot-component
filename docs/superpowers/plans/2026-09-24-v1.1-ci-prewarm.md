# v1.1 CI 构建时预生成实现计划（B2）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 spec §4.1 的「AI 静态化」：CI 构建时批量预生成开发者路径结果、序列化缓存随产物部署，运行时直接命中预生成缓存，访问时零 LLM 延迟。

**Architecture:** `MemoryCacheStore` 增加 JSON 序列化（dump/load）；`prewarm` 助手复用既有 handler（拿 fake Request 直接调，零流水线重复）；示例项目演示完整链路：prewarm 脚本 → `ai-cache.json` → server 启动时水合 → LLM 全挂时页面仍返回预生成内容。

**Tech Stack:** 同 v1。

**Spec:** `docs/superpowers/specs/2026-09-24-ai-native-rendering-sdk-design.md`（§4.1 双层缓存 L1 预生成）

## 设计决策（本次新增，评审请核验）

1. **prewarm 复用 handler 而非重写流水线**：`prewarm(handler, slotIds)` 对每个 slotId 以 fake GET Request 调用 `createAiRenderHandler` 返回的函数。编译/LLM/校验/缓存逻辑零重复，行为与线上首请求完全一致。
2. **缓存序列化为纯 JSON**（`{ key: { value, expiresAt, staleUntil } }`），load 时按调用方提供的 now 剔除已过 stale 期的条目。时间戳为绝对毫秒——CI 与部署的时钟需一致（常规假设）。
3. **预生成条目的 TTL 与线上默认一致**（开发者路径 1h + stale 1d），prewarm 可传更长 TTL——通过给 handler 传 `developerTtlMs`/`staleMs` 实现，prewarm 本身不管 TTL。

## Global Constraints

- 零运行时依赖；TS strict；`.js` 扩展名相对导入；中文注释/提交信息。
- 每个任务 TDD：先写失败测试。

---

### Task 1: proxy — 缓存序列化 + prewarm 助手

**Files:**
- Modify: `packages/proxy/src/cache.ts`
- Create: `packages/proxy/src/prewarm.ts`
- Modify: `packages/proxy/src/index.ts`
- Test: `packages/proxy/src/prewarm.test.ts`（缓存序列化测试也放这里）

**Interfaces:**
- Consumes: `MemoryCacheStore`、`lookup`、`developerCacheKey`（cache.ts）；`createAiRenderHandler`（handler.ts）。
- Produces:
  - `MemoryCacheStore.dump(): string`——JSON 序列化全部未彻底过期条目。
  - `static MemoryCacheStore.load(json: string, now: number): MemoryCacheStore`——反序列化；已过期（超过 staleUntil）条目剔除；非法 JSON 抛错（调用方自行 try）。
  - `interface PrewarmResult { ok: string[]; failed: string[] }`
  - `async function prewarm(handler: (req: Request) => Promise<Response>, slotIds: string[]): Promise<PrewarmResult>`——逐槽位 fake GET，200 记 ok，其余记 failed；单个槽位异常不中断整体。

- [ ] **Step 1: 写失败的测试**

`packages/proxy/src/prewarm.test.ts`：

```ts
import { defineRegistry } from "@ai-slot/registry";
import { describe, expect, it, vi } from "vitest";
import { MemoryCacheStore, developerCacheKey, lookup } from "./cache.js";
import { createAiRenderHandler } from "./handler.js";
import type { LLMClient } from "./llm-client.js";
import { prewarm } from "./prewarm.js";

describe("MemoryCacheStore 序列化", () => {
  it("dump/load 往返保留条目与 fresh/stale 语义", () => {
    const store = new MemoryCacheStore();
    store.set("a", { x: 1 }, 1000, 5000, 0);
    store.set("b", "v", 100, 100, 0); // 立即进入 stale
    const json = store.dump();
    const restored = MemoryCacheStore.load(json, 500);
    expect(lookup(restored, "a", 500)).toEqual({ value: { x: 1 }, status: "fresh" });
    expect(lookup(restored, "b", 500)).toEqual({ value: "v", status: "stale" });
  });

  it("load 时剔除已彻底过期的条目", () => {
    const store = new MemoryCacheStore();
    store.set("a", 1, 100, 100, 0); // staleUntil = 200
    const restored = MemoryCacheStore.load(store.dump(), 1000);
    expect(lookup(restored, "a", 1000)).toBeUndefined();
  });

  it("load 非法 JSON 抛错", () => {
    expect(() => MemoryCacheStore.load("不是 json", 0)).toThrow();
  });
});

describe("prewarm", () => {
  const registry = defineRegistry({
    components: { "hero-banner": { description: "", props: { title: "string" }, required: ["title"] } },
  });
  const slot = { slotId: "hero", originalContent: "<h1>x</h1>", contentVersion: "v1" };

  it("成功的槽位写入缓存并计入 ok，失败的计入 failed 且不中断", async () => {
    const llm: LLMClient = {
      complete: vi.fn().mockImplementation((req: { user: string }) => {
        if (req.user.includes("槽位：broken")) return Promise.reject(new Error("boom"));
        return Promise.resolve({ text: JSON.stringify({ tree: { component: "hero-banner", props: { title: "预生成" } } }) });
      }),
    };
    const store = new MemoryCacheStore();
    const handler = createAiRenderHandler({
      registry,
      llm,
      store,
      resolveSlot: (id) => (id === "hero" ? slot : id === "broken" ? { ...slot, slotId: "broken" } : null),
      now: () => 0,
    });
    const result = await prewarm(handler, ["hero", "broken", "unknown"]);
    expect(result).toEqual({ ok: ["hero"], failed: ["broken", "unknown"] });
    // 缓存中已有开发者路径结果（验证用的是 handler 内部同一套 key）
    const hit = lookup(store, developerCacheKey("hero", "v1", "1"), 0);
    expect(hit?.status).toBe("fresh");
    expect((hit?.value as { tree: { props: { title: string } } }).tree.props.title).toBe("预生成");
    // prewarm 结果可序列化、可被新 store 水合
    const hydrated = MemoryCacheStore.load(store.dump(), 0);
    expect(lookup(hydrated, developerCacheKey("hero", "v1", "1"), 0)?.status).toBe("fresh");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @ai-slot/proxy exec vitest run src/prewarm.test.ts`
Expected: FAIL（prewarm.js 不存在 / dump 不是函数）

- [ ] **Step 3: 实现**

`packages/proxy/src/cache.ts` 的 `MemoryCacheStore` 类中追加两个方法：

```ts
  /** 序列化为 JSON（绝对毫秒时间戳；CI 与部署时钟需一致）。 */
  dump(): string {
    return JSON.stringify(Object.fromEntries(this.map));
  }

  /** 反序列化；剔除调用时已彻底过期的条目。非法 JSON 抛 SyntaxError。 */
  static load(json: string, now: number): MemoryCacheStore {
    const raw = JSON.parse(json) as Record<string, CacheEntry<unknown>>;
    const store = new MemoryCacheStore();
    for (const [key, entry] of Object.entries(raw)) {
      if (now < entry.staleUntil) {
        store.map.set(key, entry);
      }
    }
    return store;
  }
```

`packages/proxy/src/prewarm.ts`：

```ts
export interface PrewarmResult {
  ok: string[];
  failed: string[];
}

/**
 * CI 构建时预生成（「AI 静态化」）：复用渲染代理 handler 逐槽位跑开发者路径，
 * 结果写入 handler 自带的缓存 store；之后 dump() 序列化随产物部署。
 * 单个槽位失败不中断整体。
 */
export async function prewarm(
  handler: (req: Request) => Promise<Response>,
  slotIds: string[],
): Promise<PrewarmResult> {
  const ok: string[] = [];
  const failed: string[] = [];
  for (const slotId of slotIds) {
    try {
      const res = await handler(new Request(`https://prewarm.local/ai-render/${slotId}`));
      if (res.ok) ok.push(slotId);
      else failed.push(slotId);
    } catch {
      failed.push(slotId);
    }
  }
  return { ok, failed };
}
```

`packages/proxy/src/index.ts` 追加：

```ts
export * from "./prewarm.js";
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm --filter @ai-slot/proxy test && pnpm --filter @ai-slot/proxy typecheck`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/proxy
git commit -m "feat(proxy): 缓存 JSON 序列化 + prewarm 预生成助手（复用 handler 流水线）"
```

---

### Task 2: 示例 — prewarm 脚本 + server 水合 + 「AI 静态化」集成测试

**Files:**
- Create: `examples/plain-html/prewarm.mjs`
- Modify: `examples/plain-html/server.mjs`（启动时若存在 `ai-cache.json` 则水合 store；mock LLM 与 handler 抽出以便脚本复用）
- Create: `examples/plain-html/e2e/prewarm.spec.ts`（Playwright：预生成后 LLM 全挂页面仍渲染 AI 内容）

**Interfaces:**
- Consumes: Task 1 的 `prewarm`、`MemoryCacheStore.load/dump`。
- Produces: 命令 `node prewarm.mjs`（生成 `ai-cache.json`）；server 环境变量 `AI_CACHE_FILE`（默认 `./ai-cache.json`，不存在则跳过水合）；e2e 第四条路径「AI 静态化」。

- [ ] **Step 1: 重构 server.mjs（抽出共享装配）**

把 mockLLM、slots、handler 装配抽到 `examples/plain-html/ai.mjs`：

```js
import { createAiRenderHandler, MemoryCacheStore } from "@ai-slot/proxy";
import { readFile } from "node:fs/promises";
import { registry } from "./registry.mjs";

/** Mock LLM：确定性输出，slotId=broken 时模拟失败。 */
export const mockLLM = {
  async complete(req) {
    if (req.user.includes("槽位：broken")) throw new Error("mock LLM failure");
    const isUser = req.user.includes("按用户要求调整");
    return {
      text: JSON.stringify({
        version: 1,
        slot: "hero",
        tree: {
          component: "hero-banner",
          props: {
            title: isUser ? "用户定制标题" : "AI 增强后的标题",
            subtitle: "由 mock LLM 生成",
          },
        },
      }),
    };
  },
};

export const slots = {
  hero: {
    slotId: "hero",
    originalContent: "<h1>我们的产品</h1><p>一个普通的产品介绍</p>",
    developerPrompt: "面向开发者受众，突出接入简单",
    contentVersion: "v1",
  },
  broken: { slotId: "broken", originalContent: "<h2>兜底：静态内容</h2>", contentVersion: "v1" },
};

/** 装配 handler；cacheFile 存在时水合预生成缓存。 */
export async function createHandler({ llm = mockLLM, cacheFile } = {}) {
  let store;
  if (cacheFile) {
    try {
      store = MemoryCacheStore.load(await readFile(cacheFile, "utf8"), Date.now());
      console.log(`[ai] 已水合预生成缓存: ${cacheFile}`);
    } catch {
      // 文件不存在或损坏：跳过水合
    }
  }
  return createAiRenderHandler({
    registry,
    llm,
    store,
    resolveSlot: (slotId) => slots[slotId] ?? null,
  });
}
```

`server.mjs` 改为从 `./ai.mjs` 导入 `createHandler`，启动时：

```js
const handler = await createHandler({ cacheFile: process.env.AI_CACHE_FILE ?? new URL("./ai-cache.json", import.meta.url).pathname });
```

其余（静态服务、/vendor/、toWebRequest）不变。server.mjs 顶部改为顶层 await 的模块（已是 ESM，`"type": "module"`）。

- [ ] **Step 2: 创建 prewarm.mjs**

`examples/plain-html/prewarm.mjs`：

```js
// CI 构建时预生成：node prewarm.mjs → ai-cache.json（随产物部署）
import { writeFile } from "node:fs/promises";
import { MemoryCacheStore, prewarm, createAiRenderHandler } from "@ai-slot/proxy";
import { registry } from "./registry.mjs";
import { mockLLM, slots } from "./ai.mjs";

const store = new MemoryCacheStore();
const handler = createAiRenderHandler({
  registry,
  llm: mockLLM, // 真实 CI 中换成 createOpenAIClient({ apiKey: process.env.OPENAI_API_KEY })
  store,
  resolveSlot: (slotId) => slots[slotId] ?? null,
  developerTtlMs: 24 * 3600_000, // 预生成条目：1 天 fresh + 7 天 stale
  staleMs: 7 * 24 * 3600_000,
});

const result = await prewarm(handler, Object.keys(slots));
const out = new URL("./ai-cache.json", import.meta.url).pathname;
await writeFile(out, store.dump());
console.log(`prewarm 完成: ok=[${result.ok}] failed=[${result.failed}] → ${out}`);
process.exit(result.failed.length > 0 && result.ok.length === 0 ? 1 : 0);
```

- [ ] **Step 3: 写 e2e 测试（先跑确认失败）**

`examples/plain-html/e2e/prewarm.spec.ts`：

```ts
import { expect, test } from "@playwright/test";

// 「AI 静态化」：预生成缓存水合后，即使 LLM 全挂，开发者路径仍返回预生成内容。
// 该 spec 使用独立的 webServer（见 playwright.config.ts 的 projects 配置）。
test("预生成缓存：LLM 不可用时 hero 仍渲染预生成内容", async ({ page }) => {
  await page.goto("/");
  const slot = page.locator('ai-slot[name="hero"]');
  await expect(slot.locator(".hero-title")).toHaveText("AI 增强后的标题");
});
```

`playwright.config.ts` 改为两个 project：默认项目（现有 mock server）与 prewarm 项目（LLM 恒失败 + 水合缓存）：

```ts
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "./e2e",
  use: { baseURL: "http://localhost:4173" },
  projects: [
    { name: "mock", testIgnore: "prewarm.spec.ts" },
    { name: "prewarm", testMatch: "prewarm.spec.ts" },
  ],
  webServer: [
    {
      command: "node server.mjs",
      port: 4173,
      reuseExistingServer: !process.env.CI,
    },
  ],
});
```

注意：两个 project 不能共用同一端口的不同 server。简化方案（采用此方案）：prewarm.spec.ts 在测试内自建 server——用 child_process 先跑 `node prewarm.mjs`，再以 `AI_LLM=down node server.mjs` 起独立端口 4174，page.goto 用绝对 URL。server.mjs 支持 `AI_LLM=down` 时注入恒失败 LLM：

```js
// ai.mjs createHandler 增加：
export const downLLM = { async complete() { throw new Error("LLM down（模拟故障）"); } };
// server.mjs：
const handler = await createHandler({
  llm: process.env.AI_LLM === "down" ? downLLM : undefined,
  cacheFile: process.env.AI_CACHE_FILE ?? new URL("./ai-cache.json", import.meta.url).pathname,
});
```

prewarm.spec.ts 最终形态：

```ts
import { expect, test } from "@playwright/test";
import { execFile } from "node:child_process";
import { promisify } from "node:util";
import { fileURLToPath } from "node:url";

const run = promisify(execFile);
const exampleDir = fileURLToPath(new URL(".", import.meta.url));

test("预生成缓存：LLM 不可用时 hero 仍渲染预生成内容", async ({ page }) => {
  // ① CI 预生成
  await run("node", ["prewarm.mjs"], { cwd: exampleDir });
  // ② 部署后 LLM 全挂
  const server = (await import("node:child_process")).spawn(
    "node",
    ["server.mjs"],
    { cwd: exampleDir, env: { ...process.env, AI_LLM: "down", PORT: "4174" } },
  );
  try {
    // 等服务就绪
    for (let i = 0; i < 50; i++) {
      try {
        const res = await fetch("http://localhost:4174/");
        if (res.ok) break;
      } catch { /* 未就绪 */ }
      await new Promise((r) => setTimeout(r, 200));
    }
    await page.goto("http://localhost:4174/");
    const slot = page.locator('ai-slot[name="hero"]');
    await expect(slot.locator(".hero-title")).toHaveText("AI 增强后的标题");
  } finally {
    server.kill();
  }
});
```

server.mjs 监听端口改为 `process.env.PORT ?? 4173`。

playwright.config.ts 保持单 webServer 单 project（mock 项目照旧跑 4173；prewarm.spec 自理 4174）：

```ts
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "./e2e",
  use: { baseURL: "http://localhost:4173" },
  webServer: {
    command: "node server.mjs",
    port: 4173,
    reuseExistingServer: !process.env.CI,
  },
});
```

- [ ] **Step 4: 运行验证**

```bash
pnpm build   # 确保 proxy dist 含 prewarm
pnpm --filter example-plain-html test:e2e
```

Expected: 4 个 e2e（3 旧 + 1 新）全绿。

- [ ] **Step 5: 清理与提交**

`ai-cache.json` 是构建产物：加入根 `.gitignore`（追加一行 `ai-cache.json`）。确认 `git status` 不含它。

```bash
git add examples .gitignore
git commit -m "feat(examples): CI 预生成演示——prewarm 脚本 + 缓存水合 + AI 静态化 e2e"
```

---

## 验收清单

- [ ] `pnpm build && pnpm typecheck && pnpm test` 全绿
- [ ] e2e 4/4（新增「AI 静态化」路径）
- [ ] prewarm 与线上请求共用同一条流水线（无重复编译/校验逻辑）
- [ ] `ai-cache.json` 不进 git
