# v1.1 Hardening 修复计划（阶段 A）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v1 终审与任务审查搁置的 8 项已知限制，不改变任何公开 API 的既有语义（只收紧/加固）。

**Architecture:** 在现有四个包内做定点修复：runtime 并发竞态与死代码、registry 校验器护栏补齐、proxy handler 顺序优化、测试噪音治理。

**Tech Stack:** 同 v1（TS strict / pnpm / Vitest / 零运行时依赖）。

**Spec:** `docs/superpowers/specs/2026-09-24-ai-native-rendering-sdk-design.md`（安全边界与降级链约束依然 binding）

## Global Constraints

- 沿用 v1 全部约束：零运行时依赖、TS strict、`.js` 扩展名相对导入、中文注释/提交信息、校验失败静默回退。
- 所有修复必须带回归测试（先红后绿）。

## 修复清单与任务映射

| # | 问题 | 任务 |
|---|---|---|
| A1 | 并发 `load()` 无竞态防护：轮询 GET 与用户 POST 交错时慢响应覆盖新内容 | Task 1 |
| A5 | microtask 首次加载修复无单测守护 | Task 1 |
| A6 | `observedAttributes` 死声明（无 attributeChangedCallback） | Task 1 |
| A2 | props 负载无总量护栏（字符串/数组无默认上限） | Task 2 |
| A3 | boolean 分支不校验 enum | Task 2 |
| A4 | `PropSchema.required?: boolean` 声明了但未消费（嵌套 object 无法表达必填） | Task 2 |
| A8 | resolveSlot 故障时用户路径无法走缓存（key 只依赖 slotId+prompt，本可先查） | Task 3 |
| A7 | handler 失败用例触发真实 console.warn，测试输出有噪音 | Task 3 |

---

### Task 1: runtime — 并发 load() 竞态防护 + microtask 单测 + 死声明清理

**Files:**
- Modify: `packages/runtime/src/ai-slot.ts`
- Modify: `packages/runtime/src/ai-slot.test.ts`

**Interfaces:**
- Consumes: 现有 `AiSlotElement`。
- Produces: `load()` 增加序号防护——过期响应（返回时已有更新的 load 发起）被丢弃，不覆盖内容；不改变任何方法签名。

- [ ] **Step 1: 写失败的测试**

向 `packages/runtime/src/ai-slot.test.ts` 追加（文件顶部已有所需 import，无需新增）：

```ts
describe("<ai-slot> 并发与时序防护", () => {
  beforeEach(() => {
    configureAiSlot({ registry });
    document.body.innerHTML = "";
  });
  afterEach(() => {
    vi.unstubAllGlobals();
  });

  it("首次加载推迟到 microtask：appendChild 同步阶段不发起 fetch", async () => {
    const fetchSpy = vi.fn(() => Promise.resolve({ ok: false, status: 503 }));
    vi.stubGlobal("fetch", fetchSpy);
    const el = document.createElement("ai-slot") as AiSlotElement;
    el.setAttribute("src", "/ai-render/hero");
    el.innerHTML = "<h1>兜底</h1>";
    document.body.appendChild(el);
    // connectedCallback 已同步执行完，但 load 被推迟到 microtask
    expect(fetchSpy).not.toHaveBeenCalled();
    await flush();
    expect(fetchSpy).toHaveBeenCalledTimes(1);
  });

  it("并发 load：慢的旧响应不覆盖快的新响应", async () => {
    let resolveSlow!: (v: unknown) => void;
    const slowResponse = new Promise((r) => { resolveSlow = r; });
    const fastTree = { component: "hero-banner", props: { title: "新内容" } };
    const fetchSpy = vi
      .fn()
      // 第一次（轮询 GET）挂起
      .mockImplementationOnce(() => slowResponse)
      // 第二次（用户 POST）快速返回
      .mockImplementationOnce(() =>
        Promise.resolve({ ok: true, json: () => Promise.resolve(aiResponse(fastTree)) }),
      );
    vi.stubGlobal("fetch", fetchSpy);
    const el = document.createElement("ai-slot") as AiSlotElement;
    el.setAttribute("src", "/ai-render/hero");
    el.setAttribute("editable", "");
    el.innerHTML = "<h1>兜底</h1>";
    document.body.appendChild(el);
    await flush(); // 第一次 load 已发起（挂起中）
    // 用户提交，触发第二次 load，快速完成
    const input = el.querySelector<HTMLInputElement>("input[name=prompt]");
    input!.value = "改一下";
    el.querySelector("form.ai-slot-editor")!.dispatchEvent(new Event("submit", { cancelable: true }));
    await flush();
    expect(el.querySelector(".hero-banner")?.textContent).toBe("新内容");
    // 慢响应随后到达：内容是「旧内容」，必须被丢弃
    resolveSlow({
      ok: true,
      json: () => Promise.resolve(aiResponse({ component: "hero-banner", props: { title: "旧内容" } })),
    });
    await flush();
    expect(el.querySelector(".hero-banner")?.textContent).toBe("新内容");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @ai-slot/runtime exec vitest run src/ai-slot.test.ts`
Expected: 「并发 load」测试 FAIL（旧响应覆盖新内容）；「microtask」测试 FAIL（当前无序号防护不影响它——若它意外通过则保留作为守护即可，不算红，继续）。

- [ ] **Step 3: 实现序号防护并删除死声明**

`packages/runtime/src/ai-slot.ts`：

1. 删除第 16 行 `static observedAttributes = ["src", "renderer", "editable", "refresh-interval"];`（无 attributeChangedCallback，是死声明）。
2. 在字段区（`private timer` 之后）加：

```ts
  private loadSeq = 0;
```

3. `load()` 方法开头（`const src = ...` 之前）加序号，并在 `setContent` 前检查：

```ts
  async load(userPrompt?: string): Promise<void> {
    const seq = ++this.loadSeq;
    const src = this.getAttribute("src");
```

把 `if (!el) return;` 与 `this.setContent(el);` 之间改为：

```ts
      if (!el) return;
      if (seq !== this.loadSeq) return; // 已有更新的 load 发起，丢弃过期响应
      this.setContent(el);
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm --filter @ai-slot/runtime test && pnpm --filter @ai-slot/runtime typecheck`
Expected: 全部 PASS（含既有 15 个 + 新增 2 个）；typecheck 通过。

- [ ] **Step 5: Commit**

```bash
git add packages/runtime
git commit -m "fix(runtime): 并发 load 序号防护（丢弃过期响应）+ microtask 守护测试 + 删除 observedAttributes 死声明"
```

---

### Task 2: registry — props 总量护栏 + boolean enum + 嵌套 required 消费

**Files:**
- Modify: `packages/registry/src/validator.ts`
- Modify: `packages/registry/src/validator.test.ts`

**Interfaces:**
- Consumes: 现有 `validateComponentTree`。
- Produces: `ValidatorOptions` 新增 `maxStringLength?: number`（默认 10_000）与 `maxArrayItems?: number`（默认 200）——字符串/数组的全局上限，与 schema 声明的上限取较严者；boolean 分支支持 enum；嵌套 object 的必填从 `PropSchema.required: true` 派生。

- [ ] **Step 1: 写失败的测试**

向 `packages/registry/src/validator.test.ts` 追加：

```ts
describe("validateComponentTree — v1.1 护栏", () => {
  it("字符串 prop 超过全局上限（默认 10000）即拒绝，即使 schema 未声明 maxLength", () => {
    const reg = defineRegistry({ components: { b: { description: "", props: { t: "string" } } } });
    expect(validateComponentTree(reg, { component: "b", props: { t: "x".repeat(10_000) } })).toEqual({ ok: true });
    const r = validateComponentTree(reg, { component: "b", props: { t: "x".repeat(10_001) } });
    expect(r.ok).toBe(false);
  });

  it("全局上限与 schema maxLength 取较严者", () => {
    const reg = defineRegistry({ components: { b: { description: "", props: { t: { type: "string", maxLength: 5 } } } } });
    expect(validateComponentTree(reg, { component: "b", props: { t: "123456" } }, { maxStringLength: 100 }).ok).toBe(false);
    expect(validateComponentTree(reg, { component: "b", props: { t: "x".repeat(50) } }, { maxStringLength: 10 }).ok).toBe(false);
  });

  it("数组 prop 超过全局上限（默认 200）即拒绝", () => {
    const reg = defineRegistry({ components: { b: { description: "", props: { a: { type: "array" } } } } });
    expect(validateComponentTree(reg, { component: "b", props: { a: Array.from({ length: 200 }, () => 1) } })).toEqual({ ok: true });
    expect(validateComponentTree(reg, { component: "b", props: { a: Array.from({ length: 201 }, () => 1) } }).ok).toBe(false);
  });

  it("boolean 分支校验 enum", () => {
    const reg = defineRegistry({ components: { b: { description: "", props: { p: { type: "boolean", enum: [true] } } } } });
    expect(validateComponentTree(reg, { component: "b", props: { p: true } })).toEqual({ ok: true });
    expect(validateComponentTree(reg, { component: "b", props: { p: false } }).ok).toBe(false);
  });

  it("嵌套 object 的必填从 PropSchema.required 派生", () => {
    const reg = defineRegistry({
      components: {
        card: {
          description: "",
          props: { cta: { type: "object", props: { label: { type: "string", required: true }, href: "string" } } },
        },
      },
    });
    const missing = validateComponentTree(reg, { component: "card", props: { cta: { href: "/x" } } });
    expect(missing.ok).toBe(false);
    if (!missing.ok) expect(missing.errors[0].message).toContain("必填");
    expect(validateComponentTree(reg, { component: "card", props: { cta: { label: "go" } } })).toEqual({ ok: true });
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @ai-slot/registry exec vitest run src/validator.test.ts`
Expected: 新增 5 个测试中至少 4 个 FAIL（全局上限 ×3、boolean enum、嵌套 required）。

- [ ] **Step 3: 实现**

`packages/registry/src/validator.ts` 三处改动：

1. `ValidatorOptions` 接口追加：

```ts
  /** 字符串 prop 全局上限（与 schema maxLength 取较严者），默认 10_000 */
  maxStringLength?: number;
  /** 数组 prop 全局上限（与 schema maxItems 取较严者），默认 200 */
  maxArrayItems?: number;
```

函数体开头（`const maxNodes = ...` 之后）加：

```ts
  const maxStringLength = options.maxStringLength ?? 10_000;
  const maxArrayItems = options.maxArrayItems ?? 200;
```

2. `validateValue` 的 string / boolean / array 分支改为：

```ts
      case "string": {
        if (typeof value !== "string") return fail(path, "props", "期望 string");
        const cap = Math.min(schema.maxLength ?? maxStringLength, maxStringLength);
        if (value.length > cap) {
          return fail(path, "props", `超过长度上限 ${cap}`);
        }
        if (schema.enum && !schema.enum.includes(value)) return fail(path, "props", "不在枚举范围内");
        return;
      }
```

```ts
      case "boolean": {
        if (typeof value !== "boolean") return fail(path, "props", "期望 boolean");
        if (schema.enum && !schema.enum.includes(value)) return fail(path, "props", "不在枚举范围内");
        return;
      }
```

```ts
      case "array": {
        if (!Array.isArray(value)) return fail(path, "props", "期望 array");
        const cap = Math.min(schema.maxItems ?? maxArrayItems, maxArrayItems);
        if (value.length > cap) {
          return fail(path, "props", `超过元素数上限 ${cap}`);
        }
        // 以下 items 循环保持不变
```

3. object 分支派生嵌套必填：

```ts
      case "object": {
        if (typeof value !== "object" || value === null || Array.isArray(value)) {
          return fail(path, "props", "期望 object");
        }
        if (schema.props) {
          const nestedRequired = Object.entries(schema.props)
            .filter(([, s]) => typeof s === "object" && (s as PropSchema).required === true)
            .map(([key]) => key);
          validateProps(schema.props as Record<string, PropSchema>, nestedRequired, value, path);
        }
        return;
      }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm --filter @ai-slot/registry test && pnpm --filter @ai-slot/registry typecheck`
Expected: 全部 PASS。

注意：既有测试「拒绝超长的字符串 prop」断言 message 包含 `maxLength` 字样——若实现改了 message 文案导致旧测试失败，把旧断言同步改为 `超过长度上限` 并在报告中说明。

- [ ] **Step 5: Commit**

```bash
git add packages/registry
git commit -m "feat(registry): props 全局总量护栏（字符串/数组默认上限）+ boolean enum + 嵌套 object 必填"
```

---

### Task 3: proxy — POST 路径缓存前置 + 测试噪音治理

**Files:**
- Modify: `packages/proxy/src/handler.ts`
- Modify: `packages/proxy/src/handler.test.ts`

**Interfaces:**
- Consumes: 现有 `createAiRenderHandler`。
- Produces: POST 用户路径在 resolveSlot 之前完成「限流 → sanitize → 缓存查找」，resolveSlot 故障/槽位不存在时已缓存的用户结果仍可用（fresh 直接返回；stale 在后续失败时兜底）；GET 行为不变。

- [ ] **Step 1: 写失败的测试**

向 `packages/proxy/src/handler.test.ts` 的 POST describe 追加：

```ts
  it("resolveSlot 故障时，已缓存的用户结果仍返回（缓存前置）", async () => {
    const llm = makeLLM(JSON.stringify({ tree: goodTree }));
    let failResolve = false;
    const handler = createAiRenderHandler({
      registry,
      llm,
      resolveSlot: () => {
        if (failResolve) throw new Error("kv down");
        return slot;
      },
      now: () => 1000,
    });
    const first = await handler(post("hello"));
    expect(first.status).toBe(200);
    failResolve = true;
    const second = await handler(post("hello"));
    expect(second.status).toBe(200); // fresh 缓存直接返回，不依赖 resolveSlot
    const third = await handler(post("从未见过的提示词"));
    expect(third.status).toBe(503); // 无缓存时仍是 503
  });
```

并在 `handler.test.ts` 顶部 describe 之前（`const slot = ...` 定义之后即可）加全局的 warn 静音，治理测试输出噪音：

```ts
// 失败路径会触发真实 console.warn（符合设计），测试中静音以保持输出干净
beforeEach(() => {
  vi.spyOn(console, "warn").mockImplementation(() => {});
});
afterEach(() => {
  vi.restoreAllMocks();
});
```

`beforeEach`/`afterEach` 需加入 vitest import（当前文件只 import 了 `describe, expect, it, vi`）。

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @ai-slot/proxy exec vitest run src/handler.test.ts`
Expected: 新增测试 FAIL（second 当前为 503）。

- [ ] **Step 3: 实现 handler 顺序调整**

把 `handler` 函数体重排为（完整替换现有函数体，从路由匹配到流水线）：

```ts
  return async function handler(req: Request): Promise<Response> {
    const match = new URL(req.url).pathname.match(/\/ai-render\/([\w-]+)\/?$/);
    if (!match) return json({ error: "not_found" }, 404);
    const slotId = match[1];

    // ① 方法与用户提示词：POST 先做限流与 sanitize（不依赖槽位）
    let userPrompt: string | undefined;
    if (req.method === "POST") {
      if (limiter) {
        const ip = req.headers.get("x-forwarded-for")?.split(",")[0]?.trim() ?? "anonymous";
        if (!limiter.check(ip, now())) return json({ error: "rate_limited" }, 429);
      }
      const body: unknown = await req.json().catch(() => null);
      const cleaned = sanitizeUserPrompt((body as { prompt?: unknown } | null)?.prompt);
      if (!cleaned) return json({ error: "invalid_prompt" }, 400);
      userPrompt = cleaned;
    } else if (req.method !== "GET") {
      return json({ error: "method_not_allowed" }, 405);
    }

    // ② 用户路径缓存前置：key 只依赖 slotId + 规范化提示词，
    //    resolveSlot 故障时已缓存结果仍可用
    let hit: ReturnType<typeof lookup<AiRenderResponse>>;
    if (userPrompt !== undefined) {
      hit = lookup<AiRenderResponse>(store, userCacheKey(slotId, userPrompt), now());
      if (hit?.status === "fresh") return json(hit.value, 200);
    }

    // ③ 解析槽位
    let slot: SlotSource | null;
    try {
      slot = await opts.resolveSlot(slotId);
    } catch (error) {
      console.warn("[ai-render] 槽位解析失败", error);
      return hit ? json(hit.value, 200) : json({ error: "ai_unavailable" }, 503);
    }
    if (!slot) return json({ error: "unknown_slot" }, 404);

    // ④ 开发者路径缓存（key 依赖 contentVersion，需在 resolveSlot 之后）
    if (userPrompt === undefined) {
      hit = lookup<AiRenderResponse>(
        store,
        developerCacheKey(slotId, slot.contentVersion, slot.promptVersion ?? "1"),
        now(),
      );
      if (hit?.status === "fresh") return json(hit.value, 200);
    }

    const isUserPath = userPrompt !== undefined;
    const key =
      userPrompt !== undefined
        ? userCacheKey(slotId, userPrompt)
        : developerCacheKey(slotId, slot.contentVersion, slot.promptVersion ?? "1");
    const ttlMs = isUserPath ? (opts.userTtlMs ?? 600_000) : (opts.developerTtlMs ?? 3_600_000);
    const staleMs = opts.staleMs ?? 86_400_000;

    const staleOr503 = (): Response => {
      if (hit) return json(hit.value, 200); // 降级链：stale 缓存兜底
      return json({ error: "ai_unavailable" }, 503);
    };

    // ⑤ LLM 流水线（保持现有 try 块不变）
    try {
      // ……现有代码原样保留……
    }
  };
```

注意：⑤ 的 try 块**逐字保留现有实现**（compilePrompt → llm.complete → onUsage → JSON.parse → validate → store.set → json），上述伪代码只为展示新顺序；实现时对现有代码做最小移动而非重写。`hit` 的类型用 `CacheLookup<AiRenderResponse> | undefined`（从 ./cache.js 导入 `type CacheLookup`）替代伪代码里的 ReturnType 写法。

行为变化确认（需在报告中说明）：POST + 未知槽位 + 有缓存 → 现在返回缓存（原 404）；POST + 未知槽位 + 无缓存 → 仍 404。405 判断移到 resolveSlot 之前（原顺序对未知槽位先 404，现在对错误方法先 405）——两条路径都是合法错误码，属可接受微调。

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm --filter @ai-slot/proxy test && pnpm --filter @ai-slot/proxy typecheck`
Expected: 全部 PASS（含既有用例——注意「超过限流返回 429」等用例的行为不变）；测试输出无 console.warn 噪音。

- [ ] **Step 5: Commit**

```bash
git add packages/proxy
git commit -m "feat(proxy): 用户路径缓存前置（resolver 故障仍可用缓存）+ 测试 console.warn 静音"
```

---

## 验收清单

- [ ] `pnpm build && pnpm typecheck && pnpm test` 全绿
- [ ] `pnpm --filter example-plain-html test:e2e` 3/3 绿
- [ ] 8 项修复全部有对应回归测试（A1/A2/A3/A4/A5/A8 新增测试；A6 删除后全套件仍绿；A7 输出干净）
